# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build output (deploy.sh rebuilds this fresh on every deploy — committing it
# just leaves the server's working tree dirty after every build, which blocks
# the next `git pull --rebase`)
dist/

# Environment variables
.env
.env.local
.env.production

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Logs
logs/
*.log
